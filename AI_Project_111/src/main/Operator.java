package main;

class Operator {
    public State apply(State state) {
        // Apply the operator to return a new state
        return new State(); // Adjust according to your problem
    }

    public int cost() {
        return 1; // Assuming each action has a unit cost, adjust as needed
    }
}
